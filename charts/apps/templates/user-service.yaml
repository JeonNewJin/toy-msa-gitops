apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.userService.replicas }}
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      serviceAccountName: user-service
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      containers:
        - name: user-service
          image: {{ .Values.global.imageRegistry }}/user-service:{{ .Values.global.imageTag }}
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: {{ .Values.userService.port.http }}
            - name: management
              containerPort: {{ .Values.userService.port.management }}
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: {{ .Values.global.springProfile }}
            - name: SERVER_PORT
              value: {{ .Values.userService.port.http | quote }}
            - name: MANAGEMENT_SERVER_PORT
              value: {{ .Values.userService.port.management | quote }}
            - name: ZIPKIN_ENDPOINT
              value: {{ .Values.zipkin.endpoint }}
            - name: LOGSTASH_HOST
              value: {{ .Values.logstash.host }}
            - name: LOGSTASH_PORT
              value: {{ .Values.logstash.port | quote }}
            - name: MYSQL_HOST
              value: {{ .Values.userService.mysql.host }}
            - name: MYSQL_PORT
              value: {{ .Values.userService.mysql.port | quote }}
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: mysql-username
            - name: MYSQL_PWD
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: mysql-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: jwt-secret
            - name: JWT_EXPIRATION
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: jwt-expiration
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: {{ .Values.userService.port.management }}
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: {{ .Values.userService.port.management }}
            initialDelaySeconds: 30
            periodSeconds: 15

---

apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - name: http
      port: {{ .Values.userService.port.http }}
      targetPort: {{ .Values.userService.port.http }}
    - name: management
      port: {{ .Values.userService.port.management }}
      targetPort: {{ .Values.userService.port.management }}
